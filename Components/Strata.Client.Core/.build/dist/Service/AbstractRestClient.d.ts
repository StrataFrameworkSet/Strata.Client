import { ICompletionStage } from "strata.foundation.core/Concurrent";
import { IResponseProcessor } from "./IResponseProcessor";
import { IHeadersConsumer } from "./IHeadersConsumer";
export declare abstract class AbstractRestClient {
    private readonly baseUrl;
    private headers;
    private consumer;
    private readonly responseProcessor;
    protected constructor(baseUrl: string, responseProcessor?: IResponseProcessor);
    setHeader(headerKey: string, headerValue: string): AbstractRestClient;
    appendHeader(headerKey: string, headerValue: string): AbstractRestClient;
    clearHeader(headerKey: string): AbstractRestClient;
    clearHeaders(): AbstractRestClient;
    getHeaderKeys(): HeadersIterator<string>;
    getHeader(headerKey: string): string;
    hasHeader(headerKey: string): boolean;
    setHeadersConsumer(consumer: IHeadersConsumer): void;
    protected doPost<TReply, TRequest>(methodPath: string, request: TRequest): ICompletionStage<TReply>;
    protected doPut<TReply, TRequest>(methodPath: string, request: TRequest): ICompletionStage<TReply>;
    protected doDelete<TReply>(methodPath: string, pathParams: Map<string, Object>, queryParams: Map<string, Object>): ICompletionStage<TReply>;
    protected doGet<TReply>(methodPath: string, pathParams: Map<string, Object>, queryParams: Map<string, Object>): ICompletionStage<TReply>;
    private sendRequestWithBody;
    private sendRequestWithParameters;
    private getUrl;
    private resolveTemplates;
    private appendQueryParams;
    private processHeaders;
    private processResponse;
    private processException;
    private isAbstractServiceReply;
}
